# Test Fixtures

Static and local HTML fixtures for unit, integration, and optional smoke checks.

Suggested layout:
- `tests/fixtures/http/` — lightweight static routes
- `tests/fixtures/pages/` — reusable HTML pages
- `tests/fixtures/` files for screenshot/network assertions
