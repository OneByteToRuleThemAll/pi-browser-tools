# Unit Tests

Goal: verify registration surface, schema consistency, and docs coverage quickly.

## Current checks
- all known tools are registered
- helper functions exist (during migration)
- README includes tool headings
- behavior/error messages remain stable

Keep these tests deterministic and fast.
